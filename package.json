{
  "name": "fetch-cf-config",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "sh scripts/deploy.sh",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "test": "node test/rollback.test.mjs && node test/versioning.test.mjs",
    "test:ui": "node test/ui-restore.e2e.mjs"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "**D1 database** — stores version metadata, the append-only audit trail, tracked zones and the audit-log watermark. Provisioned automatically during deploy; migrations run as part of the deploy script."
      },
      "SNAPSHOTS": {
        "description": "**R2 bucket** — stores the gzipped configuration payloads: full snapshots and version deltas."
      }
    }
  }
}
